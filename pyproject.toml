[project]
name = "rustinfer-python-tools"
version = "1.0.1"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
triton = ["triton==3.6.0"]
tilelang = ["tilelang==0.1.14; sys_platform == 'linux' and platform_machine == 'x86_64'"]
bench = [
    "aiohttp>=3.10,<4",
    "matplotlib>=3.9,<4",
    "numpy>=2,<3",
]
vllm-reference = ["vllm>=0.10"]
sglang-reference = ["sglang>=0.4"]
deepseek-v4-reference = [
    "torch==2.11.0",
    "transformers==5.12.0",
    "safetensors==0.8.0",
    "numpy>=2,<3",
]

[tool.uv]
package = false

# The vLLM reference engine pins its own TileLang compiler. Install the Rust AOT
# compiler separately instead of combining these optional environments.
conflicts = [
    [{ extra = "tilelang" }, { extra = "vllm-reference" }],
]
